version: '3'

services:

  db:
    image: postgres:10.1

  rabbitmq:
    image: rabbitmq

  speid:
    build: .
    tty: true
    volumes:
      - .:/speid
    links:
      - db
      - rabbitmq
    depends_on:
      - db
      - rabbitmq
    env_file:
      - .env